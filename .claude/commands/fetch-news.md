Trigger the news pipeline manually and report the results.

Steps:
1. Run: node src/pipeline.js
2. Capture the output
3. Report a summary: how many articles were fetched and saved per country, any errors encountered

If the pipeline fails, show the error and suggest what might be wrong (e.g., missing .env, Supabase connection, API key issue).
